kind = "web"
previewPath = "/"
title = "Roster — HR Management System"
version = "1.0.0"
id = "artifacts/roster"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23979

[services.development]
run = "pnpm --filter @workspace/roster run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/roster", "run", "build" ]
publicDir = "artifacts/roster/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23979"
BASE_PATH = "/"
